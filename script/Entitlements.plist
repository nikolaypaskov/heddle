<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- The com.apple.security.application-groups entitlement was removed.
         It requested "2BBY89MBSN.dev.warp", an app group belonging to Warp's Apple team, and an
         app group must be owned by the signing team. Keeping it made the bundle unsignable by any
         other Developer ID and would fail notarization.
         Nothing depends on it: warp_core::paths::app_group_container_path is its only consumer,
         via secure_state_dir, and every caller falls back to state_dir already.
         NOTE: XML comments may not contain a double hyphen. AMFI rejects the whole file if one
         appears here, while plutil lint accepts it, so a lint pass is not sufficient proof. -->
    <key>com.apple.security.automation.apple-events</key>
	<true/>
    <key>com.apple.security.device.audio-input</key>
	<true/>
	<key>com.apple.security.device.camera</key>
	<true/>
    <key>com.apple.security.personal-information.addressbook</key>
	<true/>
	<key>com.apple.security.personal-information.calendars</key>
	<true/>
	<key>com.apple.security.personal-information.location</key>
	<true/>
	<key>com.apple.security.personal-information.photos-library</key>
	<true/>
</dict>
</plist>
